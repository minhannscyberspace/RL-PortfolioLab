seed: 42

phase2_env_config_path: configs/phase2_env.yaml

benchmarks:
  - name: equal_weight
  - name: momentum
    lookback: 20
  - name: inverse_vol
  - name: mean_variance
    lookback: 60
    ridge: 1e-6

output:
  out_dir: artifacts/phase4/benchmarks
  run_name: baseline_benchmarks

